<% if @bet_type.errors.any? %>
	<div id="error_explanation">
		<div class="alert alert-error">
			The form contains <%=pluralize(@bet_type.errors.count, "error")%>
		</div>
		<ul>
		<% @bet_type.errors.full_messages.each do |msg| %>
			<li>* <%= msg %></li>
		<%end%>
		</ul>
		</div>
	<%end%>
